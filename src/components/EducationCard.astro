---
interface Props {
    imgName:string,
    year:string,
    school:string,
    career:string
}

const {imgName, year, school, career} = Astro.props;
---

<div class="flex flex-col justify-end max-w-2xl border-[1px] border-[#ABB2BF] bg-black bg-opacity-10 backdrop-filter backdrop-blur-sm lg:max-w-xs xl:max-w-sm">
    <img class="p-8 " src=`../assets/${imgName}.webp` alt={school}>
    <span class="pt-2 pb-2 pl-4 pr-4 border-t-[1px] border-b-[1px] border-[#ABB2BF]"> {year} - {school} </span>
    <span class="pt-2 pb-2 pl-4 pr-4 "> {career} </span>
</div>